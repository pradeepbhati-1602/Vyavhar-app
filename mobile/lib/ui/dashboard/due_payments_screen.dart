import 'package:flutter/material.dart';

class DuePaymentsScreen extends StatelessWidget {
  const DuePaymentsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Due Payments')),
      body: const Center(child: Text('Due Payments List - Coming Soon')),
    );
  }
}
